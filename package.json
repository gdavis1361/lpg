{
  "name": "lpg-monorepo-root",
  "private": true,
  "workspaces": [
    "lpg-ui",
    "lpg-backend"
  ],
  "scripts": {
    "dev:ui": "doppler run --project lpg --config dev --secrets-group frontend -- npm run dev --workspace=lpg-ui",
    "dev:backend": "doppler run --project lpg --config dev --secrets-group backend -- npm run supabase:start --workspace=lpg-backend",
    "dev": "npm-run-all --parallel dev:ui dev:backend",
    "build:ui": "doppler run --project lpg --config prod --secrets-group frontend -- npm run build --workspace=lpg-ui",
    "build:ui:dev": "doppler run --project lpg --config dev --secrets-group frontend -- npm run build --workspace=lpg-ui",
    "lint:ui": "npm run lint --workspace=lpg-ui",
    "lint:backend": "echo \"Linting for backend not yet configured\"",
    "lint": "npm-run-all --parallel lint:ui lint:backend",
    "types:supabase": "doppler run --project lpg --config dev --secrets-group backend -- npm run types:supabase --workspace=lpg-ui",
    "migrate:db": "doppler run --project lpg --config dev --secrets-group backend -- npm run migrate --workspace=lpg-backend",
    "install:all": "npm install --workspaces",
    "doppler:login": "doppler login",
    "doppler:setup": "doppler setup",
    "doppler:configure": "npm-run-all doppler:setup:ui doppler:setup:backend",
    "doppler:setup:ui": "cd lpg-ui && doppler setup",
    "doppler:setup:backend": "cd lpg-backend && doppler setup"
  },
  "devDependencies": {
    "doppler-cli": "^2.0.66",
    "npm-run-all": "^4.1.5"
  }
}
