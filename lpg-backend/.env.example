# Supabase Project Configuration
# These are typically found in your Supabase project settings

# Supabase API URL
SUPABASE_URL="your-supabase-url"

# Supabase Anon Key (publicly shareable)
# Used by clients to interact with your Supabase project
SUPABASE_ANON_KEY="your-supabase-anon-key"

# Supabase Service Role Key (secret, keep safe!)
# Grants full access to your Supabase project, bypassing RLS.
# Used for admin tasks, migrations, and server-to-server communication.
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Supabase JWT Secret (secret, keep safe!)
# Used to sign and verify JWTs for user authentication.
SUPABASE_JWT_SECRET="your-supabase-jwt-secret"

# PostgreSQL Connection String (for direct database access, e.g., migrations)
# Ensure this points to your Supabase database.
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
POSTGRES_URL="postgresql://postgres:your-postgres-password@db.your-project-ref.supabase.co:5432/postgres"

# Optional: If using Prisma or another ORM that requires a specific format
# POSTGRES_PRISMA_URL="your-prisma-compatible-postgres-url"

# Optional: For non-pooling connections if needed
# POSTGRES_URL_NON_POOLING="your-non-pooling-postgres-url"

# Database specific (if not included in POSTGRES_URL)
# POSTGRES_USER="postgres"
# POSTGRES_PASSWORD="your-postgres-password"
# POSTGRES_HOST="db.your-project-ref.supabase.co"
# POSTGRES_DATABASE="postgres"

# Add any other backend-specific environment variables here
# Example: API_PORT=8000

# Logging Configuration (based on project standards)
LOG_LEVEL="info" # Options: debug, info, warn, error
LOG_FORMAT="console" # Options: json, console
# LOG_FILE="/path/to/log/file" # Optional: Path to log file